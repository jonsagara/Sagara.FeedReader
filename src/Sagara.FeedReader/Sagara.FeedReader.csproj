<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net6.0</TargetFrameworks>
		<SignAssembly>True</SignAssembly>
		<AssemblyOriginatorKeyFile>key.snk</AssemblyOriginatorKeyFile>
		<PackageId>Sagara.FeedReader</PackageId>
		<Title>Sagara.FeedReader</Title>
		<Authors>Armin Reiter;jonsagara</Authors>
		<Company>Reiter ITS</Company>
		<Product>Sagara.FeedReader</Product>
		<Description>RSS and ATOM feed reader/parser. FeedReader is a .net standard 1.3 library used for reading and parsing RSS and ATOM feeds. Supports RSS 0.91, 0.92, 1.0, 2.0 and ATOM. Library tested with multiple languages and feeds.</Description>
		<Copyright>Copyright 2022</Copyright>
		<PackageProjectUrl>https://dev.azure.com/sagarasoftware/Sagara.Common/_git/Sagara.FeedReader</PackageProjectUrl>
		<RepositoryUrl>https://dev.azure.com/sagarasoftware/Sagara.Common/_git/Sagara.FeedReader</RepositoryUrl>
		<!-- 
			Version: The Version property is the value most commonly set when building .NET Core applications. It 
			controls the default values of all the version numbers embedded in the build output, such as PackageVersion 
			and AssemblyVersion so it's often used as the single source of the app/library version.
			See: https://andrewlock.net/version-vs-versionsuffix-vs-packageversion-what-do-they-all-mean/#version
		-->
		<Version>2.0.3</Version>
		<!-- 
			AssemblyVersion: Every assembly you produce as part of your build process has a version number embedded in 
			it, which forms an important part of the assembly's identity. It's stored in the assembly manifest and is 
			used by the runtime to ensure correct versions are loaded etc.
			See: https://andrewlock.net/version-vs-versionsuffix-vs-packageversion-what-do-they-all-mean/#assemblyversion
		-->
		<AssemblyVersion>2.0.0.0</AssemblyVersion>
		<!--
			FileVersion: The file version is literally the version number exposed by the DLL to the file system. It's 
			the number displayed in Windows explorer, which often matches the AssemblyVersion, but it doesn't have to. 
			The FileVersion number isn't part of the assembly identity as far as the .NET Framework or runtime are 
			concerned.
			See: https://andrewlock.net/version-vs-versionsuffix-vs-packageversion-what-do-they-all-mean/#fileversion
		-->
		<FileVersion>2.0.0.0</FileVersion>
		<PackageTags>feed rss atom</PackageTags>
		<AssemblyName>Sagara.FeedReader</AssemblyName>
		<RootNamespace>Sagara.FeedReader</RootNamespace>
		<RepositoryType>git</RepositoryType>
		<PackageIcon>icon.png</PackageIcon>
		<PackageIconUrl>https://raw.github.com/codehollow/FeedReader/master/FeedReader/icon.png</PackageIconUrl>
		<PackageLicenseFile>LICENSE</PackageLicenseFile>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<PropertyGroup Condition="'$(Configuration)|$(TargetFramework)|$(Platform)'=='Debug|net6.0|AnyCPU'">
		<DocumentationFile>bin\Debug\net6.0\CodeHollow.FeedReader.xml</DocumentationFile>
		<UseNETCoreGenerator>true</UseNETCoreGenerator>
		<NoWarn></NoWarn>
	</PropertyGroup>

	<ItemGroup>
		<None Include="..\..\LICENSE" Pack="True" PackagePath="" />
	</ItemGroup>

	<ItemGroup>
		<None Include="icon.png" Pack="True" PackagePath="\" />
	</ItemGroup>

	<ItemGroup>
	  <PackageReference Include="Microsoft.Extensions.Http" Version="6.0.0" />
	  <PackageReference Include="Microsoft.Extensions.Http.Polly" Version="6.0.9" />
	  <PackageReference Include="Microsoft.IO.RecyclableMemoryStream" Version="2.2.1" />
	  <PackageReference Include="Polly" Version="7.2.3" />
	  <PackageReference Include="Scrutor" Version="4.2.0" />
	</ItemGroup>

</Project>